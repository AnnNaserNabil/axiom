export const PAGE_SIZE = 30;

export const LIGHT_MODE = "light",
	DARK_MODE = "dark",
	AUTO_MODE = "auto";
export const DEFAULT_THEME = AUTO_MODE;

// Banner height unit: vh
// Banner height unit: vh
export const BANNER_HEIGHT = 50;  // 35% of viewport height
export const BANNER_HEIGHT_EXTEND = 20;  // Additional 30% for extended effect
export const BANNER_HEIGHT_HOME = BANNER_HEIGHT + BANNER_HEIGHT_EXTEND;  // Total 65% for home

// The height the main panel overlaps the banner, unit: rem
export const MAIN_PANEL_OVERLAPS_BANNER_HEIGHT = 1.5;

// Page width: rem
export const PAGE_WIDTH = 100;
